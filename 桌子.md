
## 模型 table

### 入座 sitDown

检测是否和当前登录的服务员密码一致。该参数可配置为开关。也就是入座是只有服务员能操作还是给普通客人。

参数：

* nums 入座人数
* password 服务员密码。

返回：
status状态中标识即可

### 通过预约手机号入座 sitDownByMobile

检测是否和当前登录的服务员密码一致。该参数可配置为开关。也就是入座是只有服务员能操作还是给普通客人。

参数：

* mobile 预约手机号
* password 服务员密码。

返回：

* isMenu int 0为没有预约菜单，否则为有预约菜单

### 获取预约菜单 getBookMenu

只能获取当天预约点的菜单，不删除当天预约的菜单。

参数：

* mobile 手机号

返回：

* list 预约时点的菜列表

### 清台 clearTable

检测是否和当前登录的服务员密码一致。该参数可配置为开关。也就是入座是只有服务员能操作还是给普通客人。

参数：

* password

返回：
status状态中标识即可。



